# Application Configuration
spring.application.name=phnomden
server.port=8081

# PostgreSQL DataSource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/wing
spring.datasource.username=piseth
spring.datasource.password=PP07071997gg
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# SQL Script Mapping
spring.jpa.mapping-resources=classpath:pd-script.xml

# Logging Configuration
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=OFF
logging.config=classpath:log4j2.xml